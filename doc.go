// Package llm defines the provider-neutral model contract shared by hosts,
// the Router, and Provider Processes. Provider-specific wire types belong in
// adapters, never in this package.
package inference
